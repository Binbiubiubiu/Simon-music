import React, { FC } from 'react';

// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface LatestMusicBlockProps {}

const LatestMusicBlock: FC<LatestMusicBlockProps> = (props) => {
  const {} = props;
  return <></>;
};

export default LatestMusicBlock;
